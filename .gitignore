# ============================================================
# podman-gitops-stack - .gitignore
# Keep the repo public-safe: ignore real inventories, secrets,
# local vault password hooks, and machine-specific files.
# ============================================================


# =========================
# OS / Editor junk
# =========================
.DS_Store
.AppleDouble
.LSOverride
*.swp
*.tmp
*.bak
*.orig
*.patch
.idea/
.vscode/
*.code-workspace


# =========================
# Python
# =========================
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.env
.env.*


# =========================
# Ansible runtime / logs
# =========================
*.retry
ansible.log
.log
.ansible/
bootstrap/ansible/.ansible/


# =========================
# Ansible Galaxy collections (vendored locally)
# We ignore the installed collections, but keep the lock/requirements file.
# =========================
bootstrap/ansible/collections/*
!bootstrap/ansible/collections/requirements.yml


# =========================
# Inventory (real files, not examples)
# =========================
# Common inventory filenames
bootstrap/ansible/inventory/hosts.yml
bootstrap/ansible/inventory/hosts.yaml
bootstrap/ansible/inventory/hosts.ini
bootstrap/ansible/inventory/inventory.yml
bootstrap/ansible/inventory/inventory.yaml
bootstrap/ansible/inventory/inventory.ini

# Private variants
bootstrap/ansible/inventory/hosts.private.yml
bootstrap/ansible/inventory/hosts.private.yaml
bootstrap/ansible/inventory/hosts.private.ini
bootstrap/ansible/inventory/hosts.local.yml
bootstrap/ansible/inventory/hosts.local.yaml
bootstrap/ansible/inventory/hosts.local.ini

# Catch-all: ignore any inventory files in this folder,
# but allow committed examples.
bootstrap/ansible/inventory/*
!bootstrap/ansible/inventory/*.example*
!bootstrap/ansible/inventory/README*


# =========================
# Ansible Vault secrets (real files, not examples)
# Only ignore the canonical vault.yml; keep vault.example.yml tracked.
# =========================
bootstrap/ansible/group_vars/**/vault.yml
bootstrap/ansible/group_vars/**/vault.yaml


# =========================
# Group vars (real files, not examples)
# Keep *.example.yml tracked; ignore environment-specific main.yml files.
# =========================
bootstrap/ansible/group_vars/**/main.yml
bootstrap/ansible/group_vars/**/main.yaml
!bootstrap/ansible/group_vars/**/main.example.yml
!bootstrap/ansible/group_vars/**/main.example.yaml


# =========================
# Vault password hooks (local-only)
# Users can choose any backend (op/file/env/etc) by creating .vault_pass.
# =========================
bootstrap/ansible/.vault_pass
bootstrap/ansible/.vault_pass.*


# =========================
# Repo-wide secrets conventions
# Ignore real secrets files, but allow examples.
# =========================
**/secrets.yml
**/secrets.yaml
**/secrets.*.yml
**/secrets.*.yaml
**/secrets_*.yml
**/secrets_*.yaml
!**/secrets.example.yml
!**/secrets.example.yaml
!**/secrets.*.example.yml
!**/secrets.*.example.yaml


# =========================
# Legacy / compatibility ignores
# =========================
.ansible_vault_pass